<!Doctype html>

<html>
<head> <title>playlister<title>
<head>
<body>
    <% @artists.each do |artist| %>
        <li><a href="/artist/<%= artist.slug %>"><%= artist.name %></a></li>
    <% end %>
</body>

</html>