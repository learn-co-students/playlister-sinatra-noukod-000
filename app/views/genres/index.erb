<h1>All Genres </h1>

<table>
  <thead>
    <th> Name </th>
  </thead>
  <tbody>
    <% @genres.each do |genre| %>
    <tr>
      <td> <a href="/genres/<%= genre.slug %>"><%= genre.name %></a> </td>
    </tr>
    <% end %>
  </tbody>
</table>
