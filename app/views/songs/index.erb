<h1>All Songs </h1>

<table>
  <thead>
    <th> Name </th>
    <th> Artist </th>
  </thead>
  <tbody>
    <% @songs.each do |song| %>
    <tr>
      <td> <a href="/songs/<%= song.slug %>"><%= song.name %></a> </td>
      <td> <a href="/artists/<%= song.artist.slug %>"><%= song.artist.name %></a> </td>
    </tr>
    <% end %>
  </tbody>
</table>
