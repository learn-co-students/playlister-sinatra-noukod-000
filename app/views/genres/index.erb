<!Doctype html>

<html>
<head> <title>playlister<title>
<head>
<body>
    <% @genres.each do |genre| %>
        <li><a href="/genres/<%= genre.slug %>"> <%= genre.name %></a></li>
    <% end %>
</body>

</html>